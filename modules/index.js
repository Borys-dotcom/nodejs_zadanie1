const randomFromRange = require('./randomFromRange');
const randomCharacters = require('./randomCharacters');
const arrayOfRandomNumbers = require('./arrayOfRandomNumbers');

module.exports = {
    randomFromRange: randomFromRange,
    randomCharacters: randomCharacters,
    arrayOfRandomNumbers: arrayOfRandomNumbers
};
